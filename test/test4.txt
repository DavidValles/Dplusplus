func integer fibo (integer n){
      if (n <= 2) {
              return 1;
                }
        var integer n1=fibo(n-1);
          var integer n2=fibo(n-2);
            return n1 + n2;
}
main {
      print("Choose fibo 1 - 40");
        var integer decision=0;
          read (decision);
            while (decision != -1) {
                        print(fibo (decision));
                    read (decision);
                          }
}

